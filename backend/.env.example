# Server Configuration
PORT=5000
NODE_ENV=development

# ─────────────────────────────────────────────────────────────
# SerpAPI — Real live price fetching via Google Shopping India
# Get your free key at https://serpapi.com (100 free searches/month)
# Leave blank or as-is to use built-in mock/sample data instead.
# ─────────────────────────────────────────────────────────────
SERPAPI_KEY=your_serpapi_key_here

# MongoDB Connection (optional - app works without it)
MONGODB_URI=mongodb://localhost:27017/kampare

# Cache TTL in seconds (how long live prices are cached)
CACHE_TTL=300

# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
