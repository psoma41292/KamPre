# Fixes the "allowedHosts[0] should be a non-empty string" error
# that appears with react-scripts 5 + newer webpack-dev-server.
HOST=localhost
PORT=3000

# Backend proxy target (must match the "proxy" field in package.json)
REACT_APP_API_URL=http://localhost:5000
